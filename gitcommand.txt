git config --global user.name "anantj-devops"
git config --global user.email "anantjain1717@gmail.com"

git init

git add . 

git add filename

git commit -m "comment msg"

git clone [URL]

git push origin <branch name>

git pull origin <branch name>

git status

git branch feature1


git switch feature1

git checkout -b feature2

git branch ( this command is used to see the available branches)





git push origin <new branch>

git add origin < remote-repo-url>
git push origin master



added this line for testing that how fetch command actually works

testing that do we ca directly push from local master to github login-feature repo 